 <br>
  <br>
<%= form_with(model: current_user.active_relationships.build, local: false) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id unless @user.nil? %></div>

  <%= f.submit "フォロー", class: "btn btn-primary" %>
<% end %>