<% @info = information %>
<%= form_with(model: @group.tags.build, local: false) do |f| %>
  <div><%= hidden_field_tag :information_id, @info.id %></div>
  <div><%= hidden_field_tag :group_id, @group.id %></div>
  <%= f.submit "タグを追加する", class: "btn btn-primary" %>
<% end %>
