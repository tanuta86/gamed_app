<%= form_with(model: current_user.favorites.find_by(group_id: @group.id),
             html: { method: :delete }, local: false) do |f| %>
  <div><%= hidden_field_tag :user_id, @user.id %></div>
  <div><%= hidden_field_tag :group_id, @group.id %></div>
   <%= f.submit "お気に入りを解除する", class: "btn btn-default" %>
<% end %>

<!--% @info = information %>-->
<!--%= form_with(model: current_user.have_informations.find_by(information_id: @info.id),-->
<!--            html: { method: :delete }, local: false) do |f| %>-->
<!--  %= f.submit "削除する", class: "btn btn-default" %>-->
<!--% end %>-->

