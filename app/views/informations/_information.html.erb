<li>

  <div>  
    <%= "#{information.content}" %>
    <!--%= render 'have_form' if logged_in? %>-->
    <% if mode == "user" %> 
      <%= render partial: 'informations/have_form', locals: { information: information } if logged_in? %>
    <% end %>
    
    <% if mode == "group" %> 
      <%= render partial: 'groups/tag_form', locals: { information: information } if logged_in? %>
    <% end %>    
    
  <div>

</li>